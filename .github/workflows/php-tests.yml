name: PHP Tests

on:
  push:
    branches:
      - main
      - staging

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello from PHP Tests
        run: |
          echo "Hello from PHP Tests!"
          sleep 60